# Core dependencies - 필수 핵심 패키지
click>=8.1.0          # CLI 프레임워크 (명령어 인터페이스)
lxml>=4.9.0           # XML 파싱 엔진 (ARXML 파일 처리)
pandas>=2.0.0         # 데이터 처리 및 분석 (테이블/통계 처리)
rich>=13.0.0          # 터미널 UI (트리뷰, 테이블, 진행바 표시)
pyyaml>=6.0           # YAML 포맷 지원 (설정 파일, 출력)

# Data validation and schema - 검증 및 스키마
jsonschema>=4.0.0     # JSON 스키마 검증
xmlschema>=2.0.0      # XML/XSD 스키마 검증 (AUTOSAR 스키마)

# Performance and optimization - 성능 최적화
tqdm>=4.65.0          # 진행 상태 표시 (대용량 파일 처리시)
cachetools>=5.3.0     # 캐싱 메커니즘 (XPath 결과 캐싱)

# Logging and debugging - 로깅 및 디버깅
colorlog>=6.7.0       # 컬러 로깅 (터미널 가독성)

# Additional formatting - 추가 포맷팅
tabulate>=0.9.0       # 테이블 포맷팅 보조
markdown>=3.4.0       # Markdown 출력 지원

# Development dependencies - 개발용 패키지
pytest>=7.3.0         # 테스트 프레임워크
pytest-cov>=4.1.0     # 테스트 커버리지 측정
pytest-mock>=3.10.0   # 모킹 지원
black>=23.0.0         # 코드 포맷터
flake8>=6.0.0         # 린터
mypy>=1.0.0           # 타입 체킹
isort>=5.12.0         # import 정렬

# Optional performance tools - 선택적 성능 도구
memory-profiler>=0.61.0  # 메모리 사용량 프로파일링
py-spy>=0.3.14           # 성능 프로파일링